---
title: Alfred
emoji: 🤵
colorFrom: indigo
colorTo: gray
sdk: gradio
sdk_version: 4.44.0
app_file: app.py
pinned: false
---

# Alfred

Ask Alfred anything — sharp, quick, and unbothered.

> When creating your Hugging Face Space, upload this file **as `README.md`** (the YAML header above is
> what tells the Space to run the Gradio app). Also upload `app.py`, `guard.py`, `persona.txt`,
> `requirements.txt`, and set the `LLM_BASE_URL` / `LLM_API_KEY` / `LLM_MODEL` secrets.
