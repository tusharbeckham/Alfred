# Skills — alfred-leader

- **orchestration** — DAG design, staffing, fan-out/fan-in, loop patterns.
- **git-workflows** — branches and worktrees for parallel/multi-repo pipelines.
- **ci-cd** — where the CI gate sits in a pipeline and how to read its result.

Domain skills (coding, debugging, security, etc.) are loaded by the workers the leader
delegates to, not by the leader itself.
