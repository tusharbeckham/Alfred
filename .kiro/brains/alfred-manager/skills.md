# Skills — alfred-manager

Skills this brain loads on demand (Brain Layer 4). Full skills live in `.kiro/skills/`.

- **orchestration** — how to frame objectives and delegate to the leader/workers.
- **self-improvement** — understanding the training loop to report progress.

The manager intentionally does NOT load deep execution skills (coding, debugging, etc.) —
those belong to the workers it delegates to. This keeps the manager's context lean.
